# <span style="color: #FFA000;">MVVM</span> 
### ConText
```c#
//.xaml
<DataContext>
    <local:ViewModel>
</DataContext>
//.xaml.cs 
    DataContext = new ViewModel();
//ViewModel.cs 
    OnPropertyChanged
```
### Binding
```c#
<TextBlock Text="{Binding Path={ViewModel.#}}"/>

<TextBlock Text="{Binding Source={StaticSource #, Path={}}"/>

<TextBox x:Name="tb"/>
<Label Content="{Binding ElementName=tb, Path={Text}}">

//RelativeSource support parrent UI control
<TextBlock Foreground="Red" Text="{Binding RelativeSource={RelativeSource Mode=Self}"/>
``` 
### Collection Notify
```c#
<ListView x:Name="lv">
    <ListView.View>
        <GridView>
            <GridViewColumn DisplayMemberBinding="{Binding #}">
        </GridView>
    </ListView.View>
</ListView>
public class ViewModel : ObservableObject {
    public ObservableCollection<DataViewModel> Datas {get; set;} = new()
    {

    }
}
```
### IValueConverter
```c#
```
### IMultiValueConverter
```c#
```
### ValidationRule
```c#
```
### ICommand
```c#
```